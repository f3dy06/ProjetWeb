/*function validateForm() {
    var nameInput = document.querySelector('.contact__form-el--name[name="name"]');
    var emailInput = document.querySelector('.contact__form-el--email[name="_replyto"]');
    var messageInput = document.querySelector('.contact__form-el--message');
  
    if (!nameInput.value || nameInput.value.length < 3 || !/^[a-zA-Z\s]+$/.test(nameInput.value)) {
      alert('Please enter a valid name with at least 3 characters containing only letters.');
      nameInput.focus();
      return false;
    }
  
    if (!emailInput.value || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(emailInput.value)) {
      alert('Please enter a valid email address.');
      emailInput.focus();
      return false;
    }
  
    if (!messageInput.value || messageInput.value.length < 10) {
      alert('Please enter a message with at least 10 characters.');
      messageInput.focus();
      return false;
    }
  
    return true;
  }*/
  